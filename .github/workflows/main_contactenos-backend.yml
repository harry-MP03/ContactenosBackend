# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions
# More info on Python, GitHub Actions, and Azure App Service: https://aka.ms/python-webapps-actions

name: Build and deploy Python app to Azure Web App - contactenos-backend

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read #This is required for actions/checkout

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python version
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Create and start virtual environment
        run: |
          python -m venv venv
          source venv/bin/activate
      
      - name: Install dependencies
        run: pip install -r requirements.txt
        
      # Optional: Add step to run tests here (PyTest, Django test suites, etc.)

      - name: Upload artifact for deployment jobs
        uses: actions/upload-artifact@v4
        with:
          name: python-app
          path: |
            .
            !venv/

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write #This is required for requesting the JWT
      contents: read #This is required for actions/checkout

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: python-app
      
      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_3897B76BC165443BB81770479643EBA0 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_94AA6B47FDB14AB080AA444B64768395 }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_CF0BA15B57EA4471A9CE3A91E57E238F }}

      - name: 'Deploy to Azure Web App'
        uses: azure/webapps-deploy@v3
        id: deploy-to-webapp
        with:
          app-name: 'contactenos-backend'
          slot-name: 'Production'
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_... }} # Esta línea ya debería estar
          app-settings-json: |
            [
              { "name": "HOST", "value": "${{ secrets.HOST }}", "slotSetting": false },
              { "name": "NAME_DATABASE", "value": "${{ secrets.NAME_DATABASE }}", "slotSetting": false },
              { "name": "USER", "value": "${{ secrets.USER }}", "slotSetting": false },
              { "name": "PASSWORD", "value": "${{ secrets.PASSWORD }}", "slotSetting": false },
              { "name": "SECRET_KEY", "value": "${{ secrets.SECRET_KEY }}", "slotSetting": false },
              { "name": "ALLOWED_HOSTS", "value": "${{ secrets.ALLOWED_HOSTS }}", "slotSetting": false },
              { "name": "DJANGO_SETTINGS_MODULE", "value": "config.settings.prod", "slotSetting": false },
              { "name": "SCM_DO_BUILD_DURING_DEPLOYMENT", "value": "true", "slotSetting": false },
              { "name": "DISABLE_COLLECTSTATIC", "value": "true", "slotSetting": false }
            ]